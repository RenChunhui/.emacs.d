;; Get the backtrace when uncaught errors occur.
(setq debug-on-error t)

;; Hit 'C-g' while it's frozen to get an Emacs Lisp backrace.
(setq debug-on-quit t)

(provide 'core-debug)

;;; core-debug.el ends here

